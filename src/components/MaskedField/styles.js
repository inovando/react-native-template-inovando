import { TextInputMask } from 'react-native-masked-text';
import styled from 'styled-components/native';
import { inputStyles } from 'components/TextField/styles';

export const Input = styled(TextInputMask)`
  ${inputStyles}
`;
